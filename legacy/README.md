# Legacy scripts

This directory preserves the two scripts that were previously stored at the repository root. They are retained for provenance only and may contain experimental preprocessing choices or research-stage code.

For the documented and tested public workflow, use the package under `src/satp/` and the command-line scripts under `scripts/`.
